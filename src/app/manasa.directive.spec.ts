import { ManasaDirective } from './manasa.directive';

describe('ManasaDirective', () => {
  it('should create an instance', () => {
    const directive = new ManasaDirective();
    expect(directive).toBeTruthy();
  });
});
